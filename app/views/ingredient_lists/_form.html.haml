= form_for @ingredient_list do |f|
  - if @ingredient_list.errors.any?
    #error_explanation
      %h2= "#{pluralize(@ingredient_list.errors.count, "error")} prohibited this ingredient_list from being saved:"
      %ul
        - @ingredient_list.errors.full_messages.each do |message|
          %li= message

  .actions
    = f.submit 'Save'
