= form_with(model: recipe, local: true) do |form|
  - if recipe.errors.any?
    #error_explanation
      %h2
        = pluralize(recipe.errors.count, "error")
        prohibited this recipe from being saved:
      %ul
        - recipe.errors.full_messages.each do |message|
          %li= message
  .field
    = form.label :title
    = form.text_field :title, id: :recipe_title
  .field
    = form.label :instructions
    = form.text_area :instructions, id: :recipe_instructions

  = form.fields_for :ingredients do |ingredients_form|
    .field
      = ingredients_form.label :name
      = ingredients_form.text_field :name, id: :name
    .field
      = ingredients_form.label :amount
      = ingredients_form.text_field :amount, id: :amount
    .field
      = ingredients_form.label :unit
      = ingredients_form.text_field :unit, id: :unit
  .actions
    = form.submit
